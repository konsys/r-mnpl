interface IModalMessage {
  title: string;
  text: string;
  buttonTitle?: string[];
}

interface IText {
  title: string;
  text: string;
  buttonTitle?: string[];
}

export const messages = {
  rollDices: {}
};
